
# sets up a new ruby version for use

function add_new_ruby(){
  MY_RUBY_VER="$1"

  sudo ruby-install ruby "$MY_RUBY_VER" --no-reinstall
  source $HOME/.zshrc

  chruby ruby-"$MY_RUBY_VER"
  gem install kubs_cli
}
